spring:
  datasource:
    url: jdbc:mysql://localhost:3306/gongnamul?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: gonguser
    password: gong1234

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080
